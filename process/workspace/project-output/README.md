# Project Output

各 Stage 的正式輸出集中放在這裡，建議使用固定子目錄：`stage0a_output/`、`stage0b_output/`、`stage0c_output/`、`stage0d_master_review/`、`stage1_output/`、`stage1_review/`、`stage2_output/`、`stage3_output/`。

正式 Validation、JSON、Storyboard、Rough Cut、Final Master 應落在對應子目錄；不要把正式輸出放進 `temp/`。